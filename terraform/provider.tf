###############################################
# Terraform + AWS Provider Configuration
###############################################

terraform {
  required_version = ">= 1.5.0"

  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 5.0"
    }
  }
}

provider "aws" {
  # Region chosen by you (Mumbai)
  region = var.aws_region
}
